# SAMWOO UI 구현 규약

이 저장소는 삼우 사내 웹 서비스의 UI 기준이자 React 19 스타터다.

## 필수 기술 기준

- React 19와 strict TypeScript를 사용한다.
- 프런트엔드에서 데이터베이스에 직접 연결하지 않는다. 모든 데이터 접근은 백엔드 API를 통한다.
- 시크릿, 토큰, 실제 접속 문자열을 코드나 `.env.example`에 넣지 않는다.
- 패키지를 추가하기 전에 기존 의존성과 브라우저 기본 기능으로 해결 가능한지 확인한다.

## 브랜드

- 파비콘과 기본 서비스 아이콘은 `/samwooeleco-favicon.png`를 사용한다.
- 밝은 배경에서는 `/footer_logo-light.png`, 어두운 배경에서는 `/footer_logo-dark.png`를 사용한다.
- 로고 비율을 변경하거나 색상을 임의로 변형하지 않는다.
- 브랜드 자산 위에 필터, 그림자, 마스크를 적용하지 않는다.

## 디자인 시스템

- 색상, 간격, 반경, 그림자는 `src/styles/tokens.css`의 의미 기반 토큰을 사용한다.
- 임의의 브랜드 색상이나 컴포넌트 변형을 만들지 말고 기존 컴포넌트를 먼저 재사용한다.
- 본문은 읽기 폭 65자 안팎, 충분한 행간, 명확한 제목 계층을 유지한다.
- 상태는 색상만으로 전달하지 않고 텍스트 또는 아이콘 레이블을 함께 제공한다.
- 장식보다 업무 흐름, 정보 구조, 다음 행동을 우선한다.

## 상호작용과 접근성

- 모든 대화형 요소에 키보드 포커스가 보여야 한다.
- 아이콘 단독 버튼은 접근 가능한 이름을 제공한다.
- hover, focus-visible, disabled, loading, empty, error 상태를 구현한다.
- `prefers-reduced-motion`을 존중한다.
- 최소 320px부터 데스크톱까지 가로 스크롤 없이 동작하게 한다.

## 완료 기준

- `npm run build`를 통과한다.
- 데스크톱과 모바일에서 실제 화면을 확인한다.
- 새 UI 패턴이 생기면 `docs/DESIGN_SYSTEM.md`와 갤러리를 함께 갱신한다.
- 배포 시 `docs/DEPLOYMENT_CACHE.md`의 정책을 적용한다. HTML·API는 장기 캐시하지
  않고 콘텐츠 해시가 있는 정적 자산만 `immutable`로 제공한다.
